node_modules/
.env
data/
test/out/
*.log

# ตั้งค่าเฉพาะเครื่องของแต่ละคน ไม่ใช่ของโปรเจกต์
.claude/settings.local.json

# ความลับสำหรับ wrangler dev ตอนพัฒนา ห้ามขึ้น git
.dev.vars

# แคช/bundle ของ wrangler ตอน dev — สร้างใหม่ได้เสมอ
.wrangler/
