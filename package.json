{
  "name": "line-oa-ai",
  "version": "1.0.0",
  "private": true,
  "description": "บอท LINE OA ตอบด้วย Gemini + หน้าหลังบ้าน 8 หน้า (Node + Express, ต่อ HTML เป็นสตริง ไม่มี build step)",
  "main": "src/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "init": "node -e \"require('./src/env').load();require('./src/store').init().then(m=>console.log('พร้อมแล้ว:',m.join(', ')||'ไฟล์ข้อมูลครบอยู่แล้ว'))\"",
    "test": "node test/admin.smoke.js",
    "shot": "node test/screenshot.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
